version: "3.9"

services:
  courses-ms:
    build:
      context: ./backend
    ports:
      - "8001:8000"

  frontend:
    build:
      context: ./frontend
    ports:
      - "3000:80"
    depends_on:
      - courses-ms
